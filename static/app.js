const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function load(){const r=await fetch('/api/dashboard');if(!r.ok){location.href='/login';return}const d=await r.json();const k=d.kpis;$('#kpis').innerHTML=`<div class="kpi"><b>${k.branches}</b><span>الفروع</span></div><div class="kpi"><b>${k.audits}</b><span>عمليات التدقيق</span></div><div class="kpi"><b>${k.average_score}%</b><span>متوسط النتيجة</span></div><div class="kpi"><b>${k.open_audits}</b><span>تدقيق مفتوح</span></div>`;$('#branches').innerHTML=d.branches.map(x=>`<div class="row"><div><b>${esc(x.name_ar)}</b><small>${esc(x.name_en)}</small></div><span>${esc(x.region)}</span></div>`).join('')||'<p>لا توجد فروع</p>';$('#audits').innerHTML=d.audits.map(x=>`<div class="row"><div><b>${esc(x.name_ar)}</b><small>${esc(x.status)}</small></div><span class="score">${x.score==null?'—':esc(x.score)+'%'}</span></div>`).join('')||'<p>لا توجد عمليات تدقيق</p>'}
$('#refresh')?.addEventListener('click',load);load().catch(()=>{$('#audits').innerHTML='<p>تعذر تحميل البيانات.</p>'});
